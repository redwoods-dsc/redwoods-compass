# UI Improvements To-Do

## 1. Add logo to questionnaire
Add the logo image (`resources/logo.svg`) to the top of the questionnaire in `index.html`.

## 2. Reposition "Visualize Results" button
Move the "Visualize Results" button to a better position in `index.html`.

## 3. Update visualization header
Change the header in `visualization.html` to match the desired branding/styling.
